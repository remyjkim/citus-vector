# Citus Cluster Configuration

# Project name (used for container naming)
COMPOSE_PROJECT_NAME=citus

# PostgreSQL authentication
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# Host authentication method (trust, md5, scram-sha-256)
# Note: 'trust' allows connections without password (development only)
POSTGRES_HOST_AUTH_METHOD=trust

# External port for coordinator node
COORDINATOR_EXTERNAL_PORT=5432

# Docker socket path (usually no need to change)
DOCKER_SOCK=/var/run/docker.sock
